services:
  app:
    image: node:24.12.0
    container_name: portfolio_app
    user: node
    working_dir: /app
    environment:
      - NODE_ENV=development
      - CHOKIDAR_USEPOLLING=true
    volumes:
      - ./app:/app
    ports:
      - "5173:5173"
    command: sleep infinity